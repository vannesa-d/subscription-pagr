function subscribe(plan) {
    alert(`You have selected the ${plan}. Proceed to payment.`);
    // Redirect to a payment page
    window.location.href = "/payment"; // Replace with your payment page link
}
